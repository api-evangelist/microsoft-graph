{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/microsoft.graph.notebookLinks",
  "title": "notebookLinks",
  "required": [
    "@odata.type"
  ],
  "type": "object",
  "properties": {
    "oneNoteClientUrl": {
      "anyOf": [
        {
          "$ref": "#/components/schemas/microsoft.graph.externalLink"
        },
        {
          "type": "object",
          "nullable": true
        }
      ],
      "description": "Opens the notebook in the OneNote native client if it's installed."
    },
    "oneNoteWebUrl": {
      "anyOf": [
        {
          "$ref": "#/components/schemas/microsoft.graph.externalLink"
        },
        {
          "type": "object",
          "nullable": true
        }
      ],
      "description": "Opens the notebook in OneNote on the web."
    },
    "@odata.type": {
      "type": "string"
    }
  }
}