naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Admin API — Admin Consent Policy
  description: 'Microsoft Graph Admin API — Admin Consent Policy. 2 operations. Lead operation: Microsoft Graph Get admin
    consent request policy. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - Admin Consent Policy
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-admin-consent-policy
    baseUri: https://graph.microsoft.com/v1.0
    description: Microsoft Graph Admin API — Admin Consent Policy business capability. Self-contained, no shared references.
    resources:
    - name: policies-adminConsentRequestPolicy
      path: /policies/adminConsentRequestPolicy
      operations:
      - name: getadminconsentrequestpolicy
        method: GET
        description: Microsoft Graph Get admin consent request policy
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updateadminconsentrequestpolicy
        method: PATCH
        description: Microsoft Graph Update admin consent request policy
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: admin-admin-consent-policy-rest
    port: 8080
    description: REST adapter for Microsoft Graph Admin API — Admin Consent Policy. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/policies/adminconsentrequestpolicy
      name: policies-adminconsentrequestpolicy
      description: REST surface for policies-adminConsentRequestPolicy.
      operations:
      - method: GET
        name: getadminconsentrequestpolicy
        description: Microsoft Graph Get admin consent request policy
        call: admin-admin-consent-policy.getadminconsentrequestpolicy
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: updateadminconsentrequestpolicy
        description: Microsoft Graph Update admin consent request policy
        call: admin-admin-consent-policy.updateadminconsentrequestpolicy
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-admin-consent-policy-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Admin API — Admin Consent Policy. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-get-admin-consent
      description: Microsoft Graph Get admin consent request policy
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: admin-admin-consent-policy.getadminconsentrequestpolicy
      outputParameters:
      - type: object
        mapping: $.
    - name: microsoft-graph-update-admin-consent
      description: Microsoft Graph Update admin consent request policy
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: admin-admin-consent-policy.updateadminconsentrequestpolicy
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
