naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Search — drives.driveItem
  description: 'Microsoft Graph Search — drives.driveItem. 1 operations. Lead operation: Microsoft Graph Invoke function search.
    Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - drives.driveItem
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: search-drives-driveitem
    baseUri: ''
    description: Microsoft Graph Search — drives.driveItem business capability. Self-contained, no shared references.
    resources:
    - name: drives-drive-id-items-driveItem-id-search(q='{q}')
      path: /drives/{drive-id}/items/{driveItem-id}/search(q='{q}')
      operations:
      - name: drivesdriveitemsdriveitemsearch
        method: GET
        description: Microsoft Graph Invoke function search
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $orderby
          in: query
          type: array
          description: Order items by property values
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: search-drives-driveitem-rest
    port: 8080
    description: REST adapter for Microsoft Graph Search — drives.driveItem. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/drives/{drive-id}/items/{driveitem-id}/search-q-q
      name: drives-drive-id-items-driveitem-id-search-q-q
      description: REST surface for drives-drive-id-items-driveItem-id-search(q='{q}').
      operations:
      - method: GET
        name: drivesdriveitemsdriveitemsearch
        description: Microsoft Graph Invoke function search
        call: search-drives-driveitem.drivesdriveitemsdriveitemsearch
        with:
          $select: rest.$select
          $orderby: rest.$orderby
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: search-drives-driveitem-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Search — drives.driveItem. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: microsoft-graph-invoke-function-search
      description: Microsoft Graph Invoke function search
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: search-drives-driveitem.drivesdriveitemsdriveitemsearch
      with:
        $select: tools.$select
        $orderby: tools.$orderby
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
