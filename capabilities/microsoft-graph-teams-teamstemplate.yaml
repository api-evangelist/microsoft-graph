naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph OData Service for namespace microsoft.graph — teams.teamsTemplate
  description: 'Microsoft Graph OData Service for namespace microsoft.graph — teams.teamsTemplate. 1 operations. Lead operation:
    Microsoft Graph Get template from teams. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - teams.teamsTemplate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-graph-teams-teamstemplate
    baseUri: https://graph.microsoft.com/v1.0
    description: Microsoft Graph OData Service for namespace microsoft.graph — teams.teamsTemplate business capability. Self-contained,
      no shared references.
    resources:
    - name: teams-team-id-template
      path: /teams/{team-id}/template
      operations:
      - name: teamsgettemplate
        method: GET
        description: Microsoft Graph Get template from teams
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: microsoft-graph-teams-teamstemplate-rest
    port: 8080
    description: REST adapter for Microsoft Graph OData Service for namespace microsoft.graph — teams.teamsTemplate. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/teams/{team-id}/template
      name: teams-team-id-template
      description: REST surface for teams-team-id-template.
      operations:
      - method: GET
        name: teamsgettemplate
        description: Microsoft Graph Get template from teams
        call: microsoft-graph-teams-teamstemplate.teamsgettemplate
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-graph-teams-teamstemplate-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph OData Service for namespace microsoft.graph — teams.teamsTemplate. One tool
      per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-get-template-teams
      description: Microsoft Graph Get template from teams
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-graph-teams-teamstemplate.teamsgettemplate
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
