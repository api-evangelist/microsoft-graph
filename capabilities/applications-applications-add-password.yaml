naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Applications API — Applications Add Password
  description: 'Microsoft Graph Applications API — Applications Add Password. 1 operations. Lead operation: Microsoft Graph
    Create Add Password. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - Applications Add Password
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: applications-applications-add-password
    baseUri: https://graph.microsoft.com/v1.0
    description: Microsoft Graph Applications API — Applications Add Password business capability. Self-contained, no shared
      references.
    resources:
    - name: applications-application-id-addPassword
      path: /applications/{application-id}/addPassword
      operations:
      - name: addpasswordapplications
        method: POST
        description: Microsoft Graph Create Add Password
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: applications-applications-add-password-rest
    port: 8080
    description: REST adapter for Microsoft Graph Applications API — Applications Add Password. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/applications/{application-id}/addpassword
      name: applications-application-id-addpassword
      description: REST surface for applications-application-id-addPassword.
      operations:
      - method: POST
        name: addpasswordapplications
        description: Microsoft Graph Create Add Password
        call: applications-applications-add-password.addpasswordapplications
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: applications-applications-add-password-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Applications API — Applications Add Password. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-create-add-password
      description: Microsoft Graph Create Add Password
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: applications-applications-add-password.addpasswordapplications
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
