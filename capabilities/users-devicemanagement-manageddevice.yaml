naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Users — deviceManagement.managedDevice
  description: 'Microsoft Graph Users — deviceManagement.managedDevice. 1 operations. Lead operation: Microsoft Graph Get
    users from deviceManagement. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - deviceManagement.managedDevice
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: users-devicemanagement-manageddevice
    baseUri: ''
    description: Microsoft Graph Users — deviceManagement.managedDevice business capability. Self-contained, no shared references.
    resources:
    - name: deviceManagement-managedDevices-managedDevice-id-users
      path: /deviceManagement/managedDevices/{managedDevice-id}/users
      operations:
      - name: devicemanagementmanageddeviceslistusers
        method: GET
        description: Microsoft Graph Get users from deviceManagement
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $orderby
          in: query
          type: array
          description: Order items by property values
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: users-devicemanagement-manageddevice-rest
    port: 8080
    description: REST adapter for Microsoft Graph Users — deviceManagement.managedDevice. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/devicemanagement/manageddevices/{manageddevice-id}/users
      name: devicemanagement-manageddevices-manageddevice-id-users
      description: REST surface for deviceManagement-managedDevices-managedDevice-id-users.
      operations:
      - method: GET
        name: devicemanagementmanageddeviceslistusers
        description: Microsoft Graph Get users from deviceManagement
        call: users-devicemanagement-manageddevice.devicemanagementmanageddeviceslistusers
        with:
          $orderby: rest.$orderby
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: users-devicemanagement-manageddevice-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Users — deviceManagement.managedDevice. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-get-users-devicemanagement
      description: Microsoft Graph Get users from deviceManagement
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: users-devicemanagement-manageddevice.devicemanagementmanageddeviceslistusers
      with:
        $orderby: tools.$orderby
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
