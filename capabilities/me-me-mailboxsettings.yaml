naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Me — me.mailboxSettings
  description: 'Microsoft Graph Me — me.mailboxSettings. 2 operations. Lead operation: Microsoft Graph Get user mailbox settings.
    Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - me.mailboxSettings
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: me-me-mailboxsettings
    baseUri: ''
    description: Microsoft Graph Me — me.mailboxSettings business capability. Self-contained, no shared references.
    resources:
    - name: me-mailboxSettings
      path: /me/mailboxSettings
      operations:
      - name: megetmailboxsettings
        method: GET
        description: Microsoft Graph Get user mailbox settings
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
      - name: meupdatemailboxsettings
        method: PATCH
        description: Microsoft Graph Update user mailbox settings
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: me-me-mailboxsettings-rest
    port: 8080
    description: REST adapter for Microsoft Graph Me — me.mailboxSettings. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/me/mailboxsettings
      name: me-mailboxsettings
      description: REST surface for me-mailboxSettings.
      operations:
      - method: GET
        name: megetmailboxsettings
        description: Microsoft Graph Get user mailbox settings
        call: me-me-mailboxsettings.megetmailboxsettings
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: meupdatemailboxsettings
        description: Microsoft Graph Update user mailbox settings
        call: me-me-mailboxsettings.meupdatemailboxsettings
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: me-me-mailboxsettings-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Me — me.mailboxSettings. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: microsoft-graph-get-user-mailbox
      description: Microsoft Graph Get user mailbox settings
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: me-me-mailboxsettings.megetmailboxsettings
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
    - name: microsoft-graph-update-user-mailbox
      description: Microsoft Graph Update user mailbox settings
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: me-me-mailboxsettings.meupdatemailboxsettings
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
