naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Contacts — contacts.orgContact.Functions
  description: 'Microsoft Graph Contacts — contacts.orgContact.Functions. 1 operations. Lead operation: Microsoft Graph Invoke
    function delta. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - contacts.orgContact.Functions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: contacts-contacts-orgcontact-functions
    baseUri: ''
    description: Microsoft Graph Contacts — contacts.orgContact.Functions business capability. Self-contained, no shared references.
    resources:
    - name: contacts-delta()
      path: /contacts/delta()
      operations:
      - name: contactsdelta
        method: GET
        description: Microsoft Graph Invoke function delta
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $orderby
          in: query
          type: array
          description: Order items by property values
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: contacts-contacts-orgcontact-functions-rest
    port: 8080
    description: REST adapter for Microsoft Graph Contacts — contacts.orgContact.Functions. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/contacts/delta
      name: contacts-delta
      description: REST surface for contacts-delta().
      operations:
      - method: GET
        name: contactsdelta
        description: Microsoft Graph Invoke function delta
        call: contacts-contacts-orgcontact-functions.contactsdelta
        with:
          $select: rest.$select
          $orderby: rest.$orderby
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: contacts-contacts-orgcontact-functions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Contacts — contacts.orgContact.Functions. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-invoke-function-delta
      description: Microsoft Graph Invoke function delta
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: contacts-contacts-orgcontact-functions.contactsdelta
      with:
        $select: tools.$select
        $orderby: tools.$orderby
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
