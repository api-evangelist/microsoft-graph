naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Oauth2permissiongrants — oauth2PermissionGrants.oAuth2PermissionGrant.Functions
  description: 'Microsoft Graph Oauth2permissiongrants — oauth2PermissionGrants.oAuth2PermissionGrant.Functions. 1 operations.
    Lead operation: Microsoft Graph Invoke function delta. Self-contained Naftiko capability covering one Microsoft Graph
    business surface.'
  tags:
  - Microsoft Graph
  - oauth2PermissionGrants.oAuth2PermissionGrant.Functions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: oauth2permissiongrants-oauth2permissiongrants-oauth2permissiongrant-functions
    baseUri: ''
    description: Microsoft Graph Oauth2permissiongrants — oauth2PermissionGrants.oAuth2PermissionGrant.Functions business
      capability. Self-contained, no shared references.
    resources:
    - name: oauth2PermissionGrants-delta()
      path: /oauth2PermissionGrants/delta()
      operations:
      - name: oauth2permissiongrantsdelta
        method: GET
        description: Microsoft Graph Invoke function delta
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $orderby
          in: query
          type: array
          description: Order items by property values
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: oauth2permissiongrants-oauth2permissiongrants-oauth2permissiongrant-functions-rest
    port: 8080
    description: REST adapter for Microsoft Graph Oauth2permissiongrants — oauth2PermissionGrants.oAuth2PermissionGrant.Functions.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/oauth2permissiongrants/delta
      name: oauth2permissiongrants-delta
      description: REST surface for oauth2PermissionGrants-delta().
      operations:
      - method: GET
        name: oauth2permissiongrantsdelta
        description: Microsoft Graph Invoke function delta
        call: oauth2permissiongrants-oauth2permissiongrants-oauth2permissiongrant-functions.oauth2permissiongrantsdelta
        with:
          $select: rest.$select
          $orderby: rest.$orderby
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: oauth2permissiongrants-oauth2permissiongrants-oauth2permissiongrant-functions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Oauth2permissiongrants — oauth2PermissionGrants.oAuth2PermissionGrant.Functions.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-invoke-function-delta
      description: Microsoft Graph Invoke function delta
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: oauth2permissiongrants-oauth2permissiongrants-oauth2permissiongrant-functions.oauth2permissiongrantsdelta
      with:
        $select: tools.$select
        $orderby: tools.$orderby
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
