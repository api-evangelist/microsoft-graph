naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Contracts — contracts.contract.Functions
  description: 'Microsoft Graph Contracts — contracts.contract.Functions. 1 operations. Lead operation: Microsoft Graph Invoke
    function delta. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - contracts.contract.Functions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: contracts-contracts-contract-functions
    baseUri: ''
    description: Microsoft Graph Contracts — contracts.contract.Functions business capability. Self-contained, no shared references.
    resources:
    - name: contracts-delta()
      path: /contracts/delta()
      operations:
      - name: contractsdelta
        method: GET
        description: Microsoft Graph Invoke function delta
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $orderby
          in: query
          type: array
          description: Order items by property values
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: contracts-contracts-contract-functions-rest
    port: 8080
    description: REST adapter for Microsoft Graph Contracts — contracts.contract.Functions. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/contracts/delta
      name: contracts-delta
      description: REST surface for contracts-delta().
      operations:
      - method: GET
        name: contractsdelta
        description: Microsoft Graph Invoke function delta
        call: contracts-contracts-contract-functions.contractsdelta
        with:
          $select: rest.$select
          $orderby: rest.$orderby
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: contracts-contracts-contract-functions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Contracts — contracts.contract.Functions. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-invoke-function-delta
      description: Microsoft Graph Invoke function delta
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: contracts-contracts-contract-functions.contractsdelta
      with:
        $select: tools.$select
        $orderby: tools.$orderby
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
