naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph OData Service for namespace microsoft.graph — Planner.planner
  description: 'Microsoft Graph OData Service for namespace microsoft.graph — Planner.planner. 2 operations. Lead operation:
    Microsoft Graph Get planner. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - Planner.planner
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-graph-planner-planner
    baseUri: https://graph.microsoft.com/v1.0
    description: Microsoft Graph OData Service for namespace microsoft.graph — Planner.planner business capability. Self-contained,
      no shared references.
    resources:
    - name: planner
      path: /planner
      operations:
      - name: plannerplannergetplanner
        method: GET
        description: Microsoft Graph Get planner
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
      - name: plannerplannerupdateplanner
        method: PATCH
        description: Microsoft Graph Update planner
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: microsoft-graph-planner-planner-rest
    port: 8080
    description: REST adapter for Microsoft Graph OData Service for namespace microsoft.graph — Planner.planner. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/planner
      name: planner
      description: REST surface for planner.
      operations:
      - method: GET
        name: plannerplannergetplanner
        description: Microsoft Graph Get planner
        call: microsoft-graph-planner-planner.plannerplannergetplanner
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: plannerplannerupdateplanner
        description: Microsoft Graph Update planner
        call: microsoft-graph-planner-planner.plannerplannerupdateplanner
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-graph-planner-planner-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph OData Service for namespace microsoft.graph — Planner.planner. One tool per
      consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-get-planner
      description: Microsoft Graph Get planner
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-graph-planner-planner.plannerplannergetplanner
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
    - name: microsoft-graph-update-planner
      description: Microsoft Graph Update planner
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: microsoft-graph-planner-planner.plannerplannerupdateplanner
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
