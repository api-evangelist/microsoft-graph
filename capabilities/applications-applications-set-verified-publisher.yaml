naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Applications API — Applications Set Verified Publisher
  description: 'Microsoft Graph Applications API — Applications Set Verified Publisher. 1 operations. Lead operation: Microsoft
    Graph Create Set Verified Publisher. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - Applications Set Verified Publisher
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: applications-applications-set-verified-publisher
    baseUri: https://graph.microsoft.com/v1.0
    description: Microsoft Graph Applications API — Applications Set Verified Publisher business capability. Self-contained,
      no shared references.
    resources:
    - name: applications-application-id-setVerifiedPublisher
      path: /applications/{application-id}/setVerifiedPublisher
      operations:
      - name: setverifiedpublisherapplications
        method: POST
        description: Microsoft Graph Create Set Verified Publisher
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: applications-applications-set-verified-publisher-rest
    port: 8080
    description: REST adapter for Microsoft Graph Applications API — Applications Set Verified Publisher. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/applications/{application-id}/setverifiedpublisher
      name: applications-application-id-setverifiedpublisher
      description: REST surface for applications-application-id-setVerifiedPublisher.
      operations:
      - method: POST
        name: setverifiedpublisherapplications
        description: Microsoft Graph Create Set Verified Publisher
        call: applications-applications-set-verified-publisher.setverifiedpublisherapplications
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: applications-applications-set-verified-publisher-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Applications API — Applications Set Verified Publisher. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-create-set-verified
      description: Microsoft Graph Create Set Verified Publisher
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: applications-applications-set-verified-publisher.setverifiedpublisherapplications
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
