naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Teams — Users.chat
  description: 'Microsoft Graph Teams — Users.chat. 3 operations. Lead operation: Microsoft Graph Get teamsApp from users.
    Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - Users.chat
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: teams-users-chat
    baseUri: ''
    description: Microsoft Graph Teams — Users.chat business capability. Self-contained, no shared references.
    resources:
    - name: users-user-id-chats-chat-id-installedApps-teamsAppInstallation-id-teamsApp
      path: /users/{user-id}/chats/{chat-id}/installedApps/{teamsAppInstallation-id}/teamsApp
      operations:
      - name: userschatsinstalledappsgetteamsapp
        method: GET
        description: Microsoft Graph Get teamsApp from users
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
    - name: users-user-id-chats-chat-id-installedApps-teamsAppInstallation-id-teamsAppDefini
      path: /users/{user-id}/chats/{chat-id}/installedApps/{teamsAppInstallation-id}/teamsAppDefinition
      operations:
      - name: userschatsinstalledappsgetteamsappdefinition
        method: GET
        description: Microsoft Graph Get teamsAppDefinition from users
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
    - name: users-user-id-chats-chat-id-tabs-teamsTab-id-teamsApp
      path: /users/{user-id}/chats/{chat-id}/tabs/{teamsTab-id}/teamsApp
      operations:
      - name: userschatstabsgetteamsapp
        method: GET
        description: Microsoft Graph Get teamsApp from users
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: teams-users-chat-rest
    port: 8080
    description: REST adapter for Microsoft Graph Teams — Users.chat. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/{user-id}/chats/{chat-id}/installedapps/{teamsappinstallation-id}/teamsapp
      name: users-user-id-chats-chat-id-installedapps-teamsappinstallation-id-teamsapp
      description: REST surface for users-user-id-chats-chat-id-installedApps-teamsAppInstallation-id-teamsApp.
      operations:
      - method: GET
        name: userschatsinstalledappsgetteamsapp
        description: Microsoft Graph Get teamsApp from users
        call: teams-users-chat.userschatsinstalledappsgetteamsapp
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users/{user-id}/chats/{chat-id}/installedapps/{teamsappinstallation-id}/teamsappdefinition
      name: users-user-id-chats-chat-id-installedapps-teamsappinstallation-id-teamsappdefini
      description: REST surface for users-user-id-chats-chat-id-installedApps-teamsAppInstallation-id-teamsAppDefini.
      operations:
      - method: GET
        name: userschatsinstalledappsgetteamsappdefinition
        description: Microsoft Graph Get teamsAppDefinition from users
        call: teams-users-chat.userschatsinstalledappsgetteamsappdefinition
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users/{user-id}/chats/{chat-id}/tabs/{teamstab-id}/teamsapp
      name: users-user-id-chats-chat-id-tabs-teamstab-id-teamsapp
      description: REST surface for users-user-id-chats-chat-id-tabs-teamsTab-id-teamsApp.
      operations:
      - method: GET
        name: userschatstabsgetteamsapp
        description: Microsoft Graph Get teamsApp from users
        call: teams-users-chat.userschatstabsgetteamsapp
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: teams-users-chat-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Teams — Users.chat. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: microsoft-graph-get-teamsapp-users
      description: Microsoft Graph Get teamsApp from users
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: teams-users-chat.userschatsinstalledappsgetteamsapp
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
    - name: microsoft-graph-get-teamsappdefinition-users
      description: Microsoft Graph Get teamsAppDefinition from users
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: teams-users-chat.userschatsinstalledappsgetteamsappdefinition
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
    - name: microsoft-graph-get-teamsapp-users-2
      description: Microsoft Graph Get teamsApp from users
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: teams-users-chat.userschatstabsgetteamsapp
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
