naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Applications API — Applications Validate Properties
  description: 'Microsoft Graph Applications API — Applications Validate Properties. 1 operations. Lead operation: Microsoft
    Graph Create Validate Properties. Self-contained Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - Applications Validate Properties
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: applications-applications-validate-properties
    baseUri: https://graph.microsoft.com/v1.0
    description: Microsoft Graph Applications API — Applications Validate Properties business capability. Self-contained,
      no shared references.
    resources:
    - name: applications-validateProperties
      path: /applications/validateProperties
      operations:
      - name: createapplicationsvalidateproperties
        method: POST
        description: Microsoft Graph Create Validate Properties
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: applications-applications-validate-properties-rest
    port: 8080
    description: REST adapter for Microsoft Graph Applications API — Applications Validate Properties. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/applications/validateproperties
      name: applications-validateproperties
      description: REST surface for applications-validateProperties.
      operations:
      - method: POST
        name: createapplicationsvalidateproperties
        description: Microsoft Graph Create Validate Properties
        call: applications-applications-validate-properties.createapplicationsvalidateproperties
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: applications-applications-validate-properties-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Applications API — Applications Validate Properties. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-create-validate-properties
      description: Microsoft Graph Create Validate Properties
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: applications-applications-validate-properties.createapplicationsvalidateproperties
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
