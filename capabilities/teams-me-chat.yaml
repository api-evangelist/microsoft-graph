naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph Teams — Me.chat
  description: 'Microsoft Graph Teams — Me.chat. 3 operations. Lead operation: Microsoft Graph Get teamsApp from me. Self-contained
    Naftiko capability covering one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - Me.chat
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: teams-me-chat
    baseUri: ''
    description: Microsoft Graph Teams — Me.chat business capability. Self-contained, no shared references.
    resources:
    - name: me-chats-chat-id-installedApps-teamsAppInstallation-id-teamsApp
      path: /me/chats/{chat-id}/installedApps/{teamsAppInstallation-id}/teamsApp
      operations:
      - name: mechatsinstalledappsgetteamsapp
        method: GET
        description: Microsoft Graph Get teamsApp from me
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
    - name: me-chats-chat-id-installedApps-teamsAppInstallation-id-teamsAppDefinition
      path: /me/chats/{chat-id}/installedApps/{teamsAppInstallation-id}/teamsAppDefinition
      operations:
      - name: mechatsinstalledappsgetteamsappdefinition
        method: GET
        description: Microsoft Graph Get teamsAppDefinition from me
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
    - name: me-chats-chat-id-tabs-teamsTab-id-teamsApp
      path: /me/chats/{chat-id}/tabs/{teamsTab-id}/teamsApp
      operations:
      - name: mechatstabsgetteamsapp
        method: GET
        description: Microsoft Graph Get teamsApp from me
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: teams-me-chat-rest
    port: 8080
    description: REST adapter for Microsoft Graph Teams — Me.chat. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/me/chats/{chat-id}/installedapps/{teamsappinstallation-id}/teamsapp
      name: me-chats-chat-id-installedapps-teamsappinstallation-id-teamsapp
      description: REST surface for me-chats-chat-id-installedApps-teamsAppInstallation-id-teamsApp.
      operations:
      - method: GET
        name: mechatsinstalledappsgetteamsapp
        description: Microsoft Graph Get teamsApp from me
        call: teams-me-chat.mechatsinstalledappsgetteamsapp
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/me/chats/{chat-id}/installedapps/{teamsappinstallation-id}/teamsappdefinition
      name: me-chats-chat-id-installedapps-teamsappinstallation-id-teamsappdefinition
      description: REST surface for me-chats-chat-id-installedApps-teamsAppInstallation-id-teamsAppDefinition.
      operations:
      - method: GET
        name: mechatsinstalledappsgetteamsappdefinition
        description: Microsoft Graph Get teamsAppDefinition from me
        call: teams-me-chat.mechatsinstalledappsgetteamsappdefinition
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/me/chats/{chat-id}/tabs/{teamstab-id}/teamsapp
      name: me-chats-chat-id-tabs-teamstab-id-teamsapp
      description: REST surface for me-chats-chat-id-tabs-teamsTab-id-teamsApp.
      operations:
      - method: GET
        name: mechatstabsgetteamsapp
        description: Microsoft Graph Get teamsApp from me
        call: teams-me-chat.mechatstabsgetteamsapp
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: teams-me-chat-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph Teams — Me.chat. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: microsoft-graph-get-teamsapp-me
      description: Microsoft Graph Get teamsApp from me
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: teams-me-chat.mechatsinstalledappsgetteamsapp
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
    - name: microsoft-graph-get-teamsappdefinition-me
      description: Microsoft Graph Get teamsAppDefinition from me
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: teams-me-chat.mechatsinstalledappsgetteamsappdefinition
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
    - name: microsoft-graph-get-teamsapp-me-2
      description: Microsoft Graph Get teamsApp from me
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: teams-me-chat.mechatstabsgetteamsapp
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
