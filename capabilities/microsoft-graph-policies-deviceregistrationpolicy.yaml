naftiko: 1.0.0-alpha2
info:
  label: Microsoft Graph OData Service for namespace microsoft.graph — policies.deviceRegistrationPolicy
  description: 'Microsoft Graph OData Service for namespace microsoft.graph — policies.deviceRegistrationPolicy. 1 operations.
    Lead operation: Microsoft Graph Get deviceRegistrationPolicy from policies. Self-contained Naftiko capability covering
    one Microsoft Graph business surface.'
  tags:
  - Microsoft Graph
  - policies.deviceRegistrationPolicy
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_GRAPH_API_KEY: MICROSOFT_GRAPH_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-graph-policies-deviceregistrationpolicy
    baseUri: https://graph.microsoft.com/v1.0
    description: Microsoft Graph OData Service for namespace microsoft.graph — policies.deviceRegistrationPolicy business
      capability. Self-contained, no shared references.
    resources:
    - name: policies-deviceRegistrationPolicy
      path: /policies/deviceRegistrationPolicy
      operations:
      - name: policiesgetdeviceregistrationpolicy
        method: GET
        description: Microsoft Graph Get deviceRegistrationPolicy from policies
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $select
          in: query
          type: array
          description: Select properties to be returned
        - name: $expand
          in: query
          type: array
          description: Expand related entities
  exposes:
  - type: rest
    namespace: microsoft-graph-policies-deviceregistrationpolicy-rest
    port: 8080
    description: REST adapter for Microsoft Graph OData Service for namespace microsoft.graph — policies.deviceRegistrationPolicy.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/policies/deviceregistrationpolicy
      name: policies-deviceregistrationpolicy
      description: REST surface for policies-deviceRegistrationPolicy.
      operations:
      - method: GET
        name: policiesgetdeviceregistrationpolicy
        description: Microsoft Graph Get deviceRegistrationPolicy from policies
        call: microsoft-graph-policies-deviceregistrationpolicy.policiesgetdeviceregistrationpolicy
        with:
          $select: rest.$select
          $expand: rest.$expand
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-graph-policies-deviceregistrationpolicy-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Graph OData Service for namespace microsoft.graph — policies.deviceRegistrationPolicy.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-graph-get-deviceregistrationpolicy-policies
      description: Microsoft Graph Get deviceRegistrationPolicy from policies
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-graph-policies-deviceregistrationpolicy.policiesgetdeviceregistrationpolicy
      with:
        $select: tools.$select
        $expand: tools.$expand
      outputParameters:
      - type: object
        mapping: $.
